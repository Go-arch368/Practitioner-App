import { FormFieldConfig } from "@/types/formField";

const viantCEIdField: FormFieldConfig = {
  name: "Viant CE ID",
  label: "Viant CE ID",
  placeholder: "Enter Viant CE ID",
  validate: (value) => {
  

    return null;
  },
};

export default viantCEIdField;
